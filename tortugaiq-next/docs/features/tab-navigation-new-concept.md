For the tortugaiq-next app for the "New Concept" modal, the navigation with the tab key is currently not very intuitive, for example, when clicking on the + New Concept button of the navigation bar, and we first see the "New Concept" modal, the first input element is displayed as focused so that the user may able to add a "Concept Name" right away, however if they press the tab key to navigate to the next input element, the cancel button is focused instead.. likewise if the user is currently on the Subject input box, and then presses tab the focus moves to an element on the browser window.. we need a plan so that navigating with the tab key is intuitive to the concept modal.

we should address what happens for example when the user is on the Subject input element and hasnt typed anything and then decides to press the tab key, or on the other hand, if the user is on the element has typed something and decides then to press on the tab key, im not sure what is the best thing to do exactly for each case, but please ccreate a plan that makes the most sense so that navigation with the tab key feels intuitive rather than random.

Feel free to improve on these ideas or expand.
